const PUBLIC_ROOT = `${process.cwd()}/public`;
const IMAGES_PATH = `${PUBLIC_ROOT}/images`;
const CONTENTS_PATH = `${PUBLIC_ROOT}/contents`;
const CAROUSEL_PATH = `${IMAGES_PATH}/carousel`;
const STORAGE_PATH = `${PUBLIC_ROOT}/storage`;

export { PUBLIC_ROOT, IMAGES_PATH, CAROUSEL_PATH, CONTENTS_PATH, STORAGE_PATH };
