const PUBLIC_ROOT = process.cwd;
const IMAGES_PATH = `${PUBLIC_ROOT}/images`;
const CONTENTS_PATH = `${PUBLIC_ROOT}/contents`;
const STORAGE_PATH = `${PUBLIC_ROOT}/storage`;

export { PUBLIC_ROOT, IMAGES_PATH, CONTENTS_PATH, STORAGE_PATH };
